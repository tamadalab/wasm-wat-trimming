# Node.js (Alpine Linux版) を使用
FROM node:20-alpine

# 作業ディレクトリ
WORKDIR /app

# AssemblyScriptコンパイラをグローバルインストール
RUN npm install -g assemblyscript

# エントリーポイントは空にしておき、docker-composeでコマンドを指定
ENTRYPOINT []